<?xml version="1.0" encoding="utf-8"?><!-- 主体为本项目定稿；空态扩展为可配置图片。 -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/background"
    style="@style/Widget.AppWidget.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.AppWidget.AppWidgetContainerParent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp">

        <LinearLayout
            android:id="@+id/rl_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_schedule_name"
                style="@style/TextAppearance.AppCompat.Medium"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLength="12"
                android:maxLines="1"
                android:text="@string/widget_preview_title"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/tv_date"
                style="@style/TextAppearance.AppCompat.Medium"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="4dp"
                android:text="@string/widget_preview_short_date"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/tv_week"
                style="@style/TextAppearance.AppCompat.Medium"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_weekday_short_6"
                android:textColor="?android:attr/colorAccent"
                android:textSize="12sp" />
        </LinearLayout>

        <ListView
            android:id="@+id/lv_course"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/rl_title"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:divider="#00000000"
            android:dividerHeight="10dp"
            android:scrollbars="none" />

        <FrameLayout
            android:id="@android:id/empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/rl_title"
            android:layout_alignParentStart="true"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginTop="8dp"
            android:gravity="center">

            <ImageView
                android:id="@+id/empty_image_compact"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:scaleType="centerCrop"
                android:visibility="gone" />

            <TextView
                android:id="@+id/empty_text_compact"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/widget_empty_today"
                android:textSize="12sp" />
        </FrameLayout>
    </RelativeLayout>

    <FrameLayout
        android:id="@+id/fl_course_left_count"
        style="@style/Widget.AppWidget.AppWidget.Container"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:layout_alignParentBottom="true"
        android:layout_gravity="bottom|center_horizontal"
        android:paddingHorizontal="5dp"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:theme="@style/Theme.AppWidget.AppWidgetContainerParent"
        android:visibility="gone">

        <!-- 使用 12dp 紧凑矢量，保持剩余课程文字与箭头的原有尺寸和垂直对齐。 -->
        <TextView
            android:id="@+id/tv_course_left_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:drawableEnd="@drawable/ic_widget_course_left_count"
            android:drawablePadding="2dp"
            android:textColor="#ff87888a"
            android:textSize="13sp" />
    </FrameLayout>
</RelativeLayout>
